# @package _global_
#
# Loaded by `python src/train.py` (@hydra `config_path="../configs"` + `train/default`).
# See configs/train/paper_experiments/README.md · When2Call: paper_experiments/function_calling/…

defaults:
  - paper_experiments/safety/paper_safety_shared

hydra:
  run:
    dir: ./outputs/paper_safety/quick_default_single_run
